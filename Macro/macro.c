#include <stdio.h>
#include <stdlib.h>

// #define polynode(type, x) {(void *) &x, NULL, NULL}

// polynode(int)

struct Node
{
    void * k;
    struct Node * left;
    struct Node * right;
};


int main()
{

}
